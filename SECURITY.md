# Sécurité

Ne publie pas une vulnérabilité contenant des données personnelles dans une issue publique. Contacte le mainteneur en privé via son profil GitHub avec la version, l’impact et une reproduction minimale.

Le projet refuse par défaut toute modification exposant l’API Coach au réseau sans authentification. Les ports restent liés à `127.0.0.1`.
